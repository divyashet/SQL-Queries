Below are sql’s which can be useful for finding relevant table names:

SELECT 'SELECT * FROM FUSION.'
  ||A.TABLE_NAME
  ||';',
  B.COLUMN_NAME,
  A.APPLICATION_SHORT_NAME
FROM FUSION.FND_TABLES A,
  FUSION.FND_COLUMNS B
WHERE A.TABLE_ID=B.TABLE_ID
AND B.COLUMN_NAME LIKE '%LOCATION_ID%'
AND A.TABLE_NAME LIKE '%ORG%'

SELECT 'SELECT * FROM FUSION.'
  ||A.TABLE_NAME
  ||';',
  B.COLUMN_NAME,
  A.APPLICATION_SHORT_NAME
FROM FUSION.FND_TABLES A,
  FUSION.FND_COLUMNS B
WHERE A.TABLE_ID=B.TABLE_ID
AND B.COLUMN_NAME LIKE '%ORGANIZATION_ID%'
AND A.TABLE_NAME LIKE '%ORG%'
AND A.APPLICATION_SHORT_NAME='PER'
